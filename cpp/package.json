{
    "name": "fountain-tools-cpp",
    "version": "1.0.0",
    "scripts": {
        "clean": "rm -rf build",
        "build": "cmake -DBUILD_SHARED_LIBS=ON -S . -B build && cmake --build build",
        "test": "npm run build && cd build && ctest"
    }
  }
  